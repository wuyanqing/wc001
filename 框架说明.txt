解决方案共分6大类库:
1、indows.Forms 属于前台引用主题模板（基本不用修改）
2、WSV      前台界面层
3、WSV.BLL  方法层
4、WSV.Control 重写的一些控件（基本不用改动）
5、WSV.DAL   所有的sql语句都集中在这层
6、WSV.Model  物理数据层，跟数据库表对应，还有一些虚拟表

cofing 所有配置都放在这个文件里，包括数据库连接和一些标识

框架工作原理：前台触发事件，然后根据需要引用方法层方法，方法层需要引用dal层的sql语句和model层的数据属性